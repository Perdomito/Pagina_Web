// RUTAS DE MIEMBROS
/*
const express = require('express');
const router = express.Router();
const { verifyToken } = require('../middleware/auth');
const miembrosController = require('../controllers/miembrosController');

router.get('/', verifyToken, miembrosController.getAll);
router.get('/:id', verifyToken, miembrosController.getById);
router.post('/', verifyToken, miembrosController.create);
router.put('/:id', verifyToken, miembrosController.update);
router.delete('/:id', verifyToken, miembrosController.delete);

module.exports = router;
*/
module.exports = {};
